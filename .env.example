# Created automatically when you add a Vercel Blob store to your project
# (Storage -> Create Database -> Blob). Run `vercel env pull` to fetch it.
BLOB_READ_WRITE_TOKEN=

# Password required to upload a new paper from the /upload page.
# Set this to something private in your Vercel project's Environment Variables.
UPLOAD_PASSWORD=changeme
